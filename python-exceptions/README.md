# python-exceptions

This project covers exception handling in Python, including
try/except blocks, raising custom exceptions, and writing
defensive functions that fail gracefully instead of crashing.
Tasks focus on safely accessing data structures (like lists and
dictionaries) without relying on built-ins such as len() when
explicitly restricted.
