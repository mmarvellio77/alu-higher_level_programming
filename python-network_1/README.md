# python-network_1

Python scripts using `urllib` to make HTTP requests and interact with APIs.

## Tasks

0. `0-hbtn_status.py` - Fetches https://alu-intranet.hbtn.io/status and displays the response body, its type, raw content, and UTF-8 decoded content.
